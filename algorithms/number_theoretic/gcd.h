#include <bits/stdc++.h>

namespace dsa{
    //uses euclidean algorithm to compute gcd
    long long gcd(long long a, long long b);
    //uses binary euclidean algorithm to compute gcd
    long long bgcd(long long a, long long b);
}
